<%= form_with model: [ article, Comment.new ] do |form| %>
  <div class='form-group'>
    <%= form.label 'Your comment:', for: :content %> <br/>
    <%= form.rich_text_area :content, class: 'form-control h5' %>
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
